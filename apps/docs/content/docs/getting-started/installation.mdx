---
title: Installation
description: Pick an adapter to get started, then add packages for code generation or ts-rest as needed.
---

## Choose an adapter

Install the adapter package for the data-fetching library you use.

### React Query

<Tabs items={['pnpm', 'npm', 'yarn']} groupId="package-manager" persist>
  <Tab value="pnpm">
    ```sh
    pnpm add @callsheet/react-query @tanstack/react-query
    ```
  </Tab>

{/* prettier-ignore */}
<Tab value="npm">
  ```sh npm install @callsheet/react-query @tanstack/react-query ```
</Tab>

  <Tab value="yarn">
    ```sh
    yarn add @callsheet/react-query @tanstack/react-query
    ```
  </Tab>
</Tabs>

### SWR

<Tabs items={['pnpm', 'npm', 'yarn']} groupId="package-manager" persist>
  <Tab value="pnpm">
    ```sh
    pnpm add @callsheet/swr swr
    ```
  </Tab>

{/* prettier-ignore */}
<Tab value="npm">```sh npm install @callsheet/swr swr ```</Tab>

  <Tab value="yarn">
    ```sh
    yarn add @callsheet/swr swr
    ```
  </Tab>
</Tabs>

The adapter package includes everything you need to define and use calls.

## Add code generation (optional)

If you want Callsheet to generate calls from your existing typed sources, add the codegen package:

<Tabs items={['pnpm', 'npm', 'yarn']} groupId="package-manager" persist>
  <Tab value="pnpm">
    ```sh
    pnpm add -D @callsheet/codegen
    ```
  </Tab>

{/* prettier-ignore */}
<Tab value="npm">```sh npm install -D @callsheet/codegen ```</Tab>

  <Tab value="yarn">
    ```sh
    yarn add @callsheet/codegen -D
    ```
  </Tab>
</Tabs>

### GraphQL

For GraphQL apps with code-generation, Callsheet can build from GraphQL Code Generator output.

For more on GraphQL setup, read [Using Callsheet with GraphQL](/getting-started/using-callsheet-with-graphql).

### ts-rest

For ts-rest apps, add `@ts-rest/core` and Callsheet will build from your existing contracts.

<Tabs items={['pnpm', 'npm', 'yarn']} groupId="package-manager" persist>
  <Tab value="pnpm">
    ```sh
    pnpm add @ts-rest/core
    ```
  </Tab>

{/* prettier-ignore */}
<Tab value="npm">```sh npm install @ts-rest/core ```</Tab>

  <Tab value="yarn">
    ```sh
    yarn add @ts-rest/core
    ```
  </Tab>
</Tabs>

#### Manual ts-rest calls[!toc]

If you are wrapping `ts-rest` routes by hand instead of using code generation, add the `ts-rest` source package:

<Tabs items={['pnpm', 'npm', 'yarn']} groupId="package-manager" persist>
  <Tab value="pnpm">
    ```sh
    pnpm add @callsheet/ts-rest @ts-rest/core
    ```
  </Tab>

{/* prettier-ignore */}
<Tab value="npm">```sh npm install @callsheet/ts-rest @ts-rest/core ```</Tab>

  <Tab value="yarn">
    ```sh
    yarn add @callsheet/ts-rest @ts-rest/core
    ```
  </Tab>
</Tabs>

For more details on `ts-rest` setup, read the [`ts-rest` guide](/guides/ts-rest).

## Callsheet Packages

- `@callsheet/react-query`: adapter for React Query apps
- `@callsheet/swr`: adapter for SWR apps
- `@callsheet/codegen`: automatically generate Callsheet from sources
- `@callsheet/ts-rest`: for manual `ts-rest` integration

## Next Steps

If you're ready to get started with Callsheet, go to [Quickstart](/getting-started/quickstart).

If you want a deeper package breakdown, go to [Packages and Imports](/reference/packages-and-imports).
